import styled from 'styled-components'
import { Alert as AlertMUI } from '@material-ui/core'

export const Alert = styled(AlertMUI)`
    && {
        font-size: 1.2vw;
        font-weight: 500;
    }
`